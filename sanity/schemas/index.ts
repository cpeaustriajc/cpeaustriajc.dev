import postSchema from './post.schema';
import aboutSchema from './about.schema';
import blogSchema from './blog.schema';

const schemas = [postSchema, aboutSchema, blogSchema];

export default schemas;
