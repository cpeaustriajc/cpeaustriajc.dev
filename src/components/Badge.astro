---
type Props = {
  icon: string;
  title: string;
  description: string;
};

const { icon, title, description } = Astro.props;
---

<div>
  <iconify-icon
    icon={icon}
    width="1.5em"
    height="1.5em"
    inline
    alt={`Icon for ${title}`}></iconify-icon>
  <div>
    <p>{title}</p>
    <p>{description}</p>
  </div>
</div>
<script src="https://code.iconify.design/iconify-icon/1.0.7/iconify-icon.min.js"
></script>
